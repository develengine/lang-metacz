

We need some way of specifying modules and namespaces:
- A hard requirement is that metacz can be used as a shell without much external context.
  - The internal interface then also needs to be fairly agnostic of how it is being used.
- This also touches on how global context for a given function is handled.
  - Functions from different namespaces will expect different global states.

- Maybe for now, if code is required, it still needs to be recorded throught the interface.
  - here the reasonable solution would be a state for which namespace / environment the code is comming from.
  - However, for later, we will need some way of merging the states of multiple interface contexts.

- We might consider the full context to have 2 different states: active and passive.
  - Passive state may not be further expanded and mutated, but an active one can.

- The only way to introduce new functions to the code, is to create an object and call the methods on it.
  - This should then implement a lookup / mapping from the requested methods to the actual methods.
  - This does need some way of identifying the individual methods in an absolute fasion.
    - Something like string identifiers... at least.
  - This then requires a way of exporting the method under a specific identifier.

A processor program could consume a stream of metacz commands and decide what needs to happen based on that.
  - Meta programming.
  - Shell.
  - Type checking.
- We thus need to be able to define this processor in metacz and have it be able to take over.

For now, we will just create a basic path to metaprogramming.
- We will create a cz object, which can be passed as a parameter to the main function,
  and then the program will be able to mutate it and build up another program.
- We can also pass in an object that allows one to execute the recorded code.
  - Maybe 2. One that compiles the code, the other that then takes the compiled code and executes it.
- This will be the meta extension.
- Maybe the cz object should provide a method for executing the given function.

Addressing functions and structures from a different module might be done at compile time.
 - This would make sense, since we can then just use functions and types and everithing as normal.
   - For instance, a basic subscript call notation requires type checking as we go.
 - For the metaprogramming extension we can just create a special instruction 'intrinsic'.
  - Then we might think of creating some way of retrieving the intrinsic codes as if they are separated into modules.

proper:
- Access to type information of the expressions' call (in a generator).
  - i32 a = 6 + gen(123)
- 

A list of problems that a programming language should enable solving:


* We must be able to generate code that can also be referenced from the frontend.

Generating code on the level of IR is all well and good,
but we also need to be able to generate code that can then be used in the fronted,
like functions that can be called by an identifier, enumerations and variables.

* We do also want to be able to add meta-code in the middle of a procedure.

Something like:

switch type {
    ${
        for type in types {
            name := type.name;
            *{
                case types.$(types.value): {
                    print("{$name}\n");
                }
            *}
        }
    $}
}

* Instead of macros we should have generating functions,
  but then we need to differentiate between which function is a generator and which not,
  so that when we call it we call it during parsing and vice versa.
  - But the generator may also behave as a function...
  - This means that the generator needs a function signature as well? For the return type?
    - No, the compiler, thus the type checker as well, isn't executing whilst we are just generating code.
    - This will be checked during compilation and we can if we want store the stack trace as metadata in the instruction stream.
  - I think that some sort of meta checker for the generators and type agnostic procedures can be implemented on top not within.

proc normal_func(i: int, d: float) -> float
{
    return float(3 * i) / d;
}

proc another_func(l: float, a: float) -> (x: float, y: float)
{
    return (cos(a) * l, sin(a) * l);
}

gen print(args: ...)
{
    ..
}

What should be visible to the generator code:
- Input arguments, So far defined variables, results.
- Structures and types, lengths of given arrays.
- External arguments from the frontend / wherever the code is being called from.


* We need to at least consider how async routines could be handled and if we need to think of anything special.
